[global]
timeout = 600

# Прокси PIP репозиторий
# index_url = https://{LOGIN}@inno.local:{PASSWORD}@sfera-t1.ru/app/ppbd-proxy/api/repository/DIGITAL_ASSISTANT-pypi-proxy-group/simple
index_url = https://${PIP_LOGIN}:${PIP_PASSWORD}@${PIP_PROXY_REPO}

# Приватный PIP репозиторий
# extra-index-url = https://{LOGIN}@inno.local:{PASSWORD}@sfera-t1.ru/app/ppbd-misc/api/repository/DIGITAL_ASSISTANT-pypi-lib/simple
extra-index-url = https://${PIP_LOGIN}:${PIP_PASSWORD}@${PIP_PRIVATE_REPO}

# Доверенный хост
# trusted-host = sfera-t1.ru
trusted-host = ${PIP_TRUSTED_HOST}